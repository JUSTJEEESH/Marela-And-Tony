{
  "name": "marcela-and-tony",
  "version": "1.0.0",
  "private": true,
  "description": "Marcela & Tony — live acoustic music on Roatán, Honduras. Static marketing + booking site.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "serve": "node build.js && node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
